---
title: "Service"
toc: true
page-layout: full
listing:
  contents:
    - "../service/**/*.qmd"
    - "!../index.qmd"
  sort:
    - "date desc"
    - "title"
  type: table
  categories: true
  date-format: "YYYY"
  sort-ui: true
  filter-ui: true
  fields: [title, year-range, description]
  field-display-names:
    description: "Role(s)"
    title: "Venue"
    year-range: "Year(s)"
  field-links: []
  feed:
    title: "Professional Service"
execute:
  freeze: auto
---

```{python}
from pathlib import Path
from rich.console import Console

console = Console()
service_dir = Path(".")
subdirs = [sdir for sdir in service_dir.iterdir() if sdir.is_dir()]
service_count = len(subdirs)

console.print(
    f":rocket: I'm thankful for the opportunity to serve  {service_count} venues!"
)
```
